export const SET_POINT_SHORT = "Ideal para desempates (tie break) o partidos cortos con amigos" as const;
export const SET_POINT_STANDARD = "Formato estandar para partidos oficiales o competitivos" as const;

export default {
    SET_POINT_SHORT,
    SET_POINT_STANDARD
} as const;